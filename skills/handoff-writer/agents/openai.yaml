interface:
  display_name: "Handoff Writer"
  short_description: "Summarize key aspects of a session"
  default_prompt: "Use $handoff-writer to summarize the key aspects of the current session for a fresh agent."
